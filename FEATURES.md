# 功能特性说明

## 核心功能

### 1. 首页 Dashboard (/)

**功能亮点**:
- 系统欢迎页面，渐变色背景设计
- 实时统计卡片：总任务数、运行任务、完成任务、失败任务
- 最近任务列表，快速查看最新的 5 个任务
- 统计卡片悬停动效
- 一键跳转到任务管理页面

**技术实现**:
- 使用 Element Plus 的 Card、Table、Progress 组件
- 响应式布局，使用 el-row 和 el-col
- 自动加载任务数据并统计

### 2. 文件上传 (/upload)

**功能亮点**:
- 拖拽上传，支持点击上传
- 支持多种文件格式：CSV、Excel (.xlsx/.xls)、JSON
- 文件大小限制：50MB
- 文件类型卡片展示
- 上传成功自动跳转到任务管理页面

**技术实现**:
- 使用 Element Plus Upload 组件
- 文件类型和大小验证
- FormData 方式上传
- 上传进度提示

### 3. 任务管理 (/tasks)

**功能亮点**:
- 任务列表表格展示
- 任务状态标签（等待中、运行中、已完成、失败）
- 实时进度条显示
- 多种操作按钮：
  - 启动任务（仅等待中的任务）
  - 停止任务（仅运行中的任务）
  - 查看进度
  - 查看日志
  - 下载结果（仅已完成的任务）
- 操作确认对话框
- 一键刷新

**技术实现**:
- 使用 Element Plus Table 组件
- 动态按钮显示（根据任务状态）
- 二次确认对话框（ElMessageBox）
- 文件下载使用 Blob 和 URL.createObjectURL

### 4. 任务进度 (/task-progress)

**功能亮点**:
- 实时进度条（百分比显示）
- 当前阶段标签
- 任务状态展示
- 处理步骤可视化（Steps 组件）
- 阶段信息提示框
- 自动刷新（每 3 秒）
- 支持 URL 参数传递任务ID

**技术实现**:
- 使用 Element Plus Progress、Steps、Descriptions 组件
- setInterval 定时刷新
- 组件卸载时清除定时器
- 根据阶段自动高亮步骤

**处理阶段**:
1. 文件解析 (PARSING)
2. 数据清洗 (CLEANING)
3. 数据处理 (PROCESSING)
4. 结果导出 (EXPORTING)
5. 完成 (FINISHED)

### 5. 日志查看 (/logs)

**功能亮点**:
- 根据任务ID查询日志
- 控制台风格显示（黑色背景）
- 日志级别颜色区分：
  - [ERROR] - 红色
  - [WARN] - 橙色
  - [INFO] - 蓝色
  - [SUCCESS] - 绿色
- 自动滚动到底部
- 日志条数统计
- 一键刷新
- 支持 URL 参数传递任务ID

**技术实现**:
- 使用 Monospace 字体
- 自定义滚动条样式
- 日志级别正则匹配
- nextTick 后滚动到底部

## 设计特色

### UI 设计
- 左侧导航栏：深色主题 (#304156)
- 主内容区：浅灰背景 (#f0f2f5)
- 卡片阴影效果
- 响应式布局

### 交互设计
- 悬停动效
- 加载状态提示
- 操作二次确认
- 成功/失败消息提示
- 平滑过渡动画

### 用户体验
- 面包屑导航（菜单高亮）
- 快捷操作按钮
- 自动跳转
- 实时数据刷新
- 友好的空状态提示

## 技术亮点

### 1. 模块化设计
- API 接口统一封装
- 路由配置独立
- 组件化开发

### 2. 请求拦截
- Axios 统一配置
- 响应拦截处理
- 错误统一提示

### 3. 代码规范
- Vue 3 Composition API
- ES6+ 语法
- 响应式数据管理

### 4. 性能优化
- 按需加载
- 定时器清理
- 合理的刷新频率

### 5. 开发体验
- Vite 快速构建
- 热模块替换
- 开发代理配置

## 扩展性

### 可扩展功能
- 用户认证和权限管理
- 任务筛选和搜索
- 任务详情页面
- 数据可视化图表
- 任务历史记录
- 批量操作
- 导出报表

### 可配置项
- API 基础路径
- 刷新间隔时间
- 文件大小限制
- 支持的文件类型
- 主题颜色

## 浏览器兼容性

- Chrome (推荐)
- Firefox
- Safari
- Edge

建议使用最新版本的现代浏览器以获得最佳体验。
